using UnityEngine;
using System.Collections;

public class DistanceCal : MonoBehaviour {
	/*
	Transform tr;
	Transform tr2;
	Rigidbody rb;
	Rigidbody rb2;
	float distance;

	// Use this for initialization
	void Start () 
	{
		tr = GameObject.Find ("Hinge_top").GetComponent<Transform> ();
		tr2 = GameObject.Find ("Hinge_bottom").GetComponent<Transform> ();

		rb = GameObject.Find ("Hinge_bottom").GetComponent<Rigidbody> ();
		rb2 = GameObject.Find ("Hinge_top").GetComponent<Rigidbody> ();

	}
	
	// Update is called once per frame
	void Update () 
	{
		//distance = Vector3.Distance (tr.transform.position, tr2.transform.position);
		if (Touch_script.name.StartsWith("top") 
			rb.isKinematic = false;
			rb.useGravity = true;
			rb2.isKinematic = true;
			rb2.useGravity = false;

		}
		if (Touch_script.name == "Hinge_bottom") 
		{

			rb.isKinematic = true;
			rb.useGravity = false;
			rb2.isKinematic = false;
			rb2.useGravity = true;
		}

	}
*/
}
